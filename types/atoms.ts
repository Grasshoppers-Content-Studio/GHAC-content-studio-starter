export interface Image {
  title: string;
  description: string;
  url: string;
}

export interface BasicImage {
  src: string;
  alt: string;
}

export interface CTA {
  label: string;
  href: string;
}
